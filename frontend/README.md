# Frontend (placeholder)

Future **Netlify**-hosted web UI and/or static docs.

- Site-specific config lives here when introduced.
- `netlify.toml.example` (if present) is **non-operative** until a real site is authorized.

No deployment is active for M00.
