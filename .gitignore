# Company secrets — tracked in-repo (do not gitignore whole dirs; they are governance/prompt canon):
#   docs/company_secrets/{prompts,manuals,enhancements,rediai33,milestones/…}
# Only ignore truly local / machine-specific paths under milestones:
docs/company_secrets/milestones/M02/_local_runs/
docs/company_secrets/milestones/M02/_local_maps/

# Cursor
.cursorrules

# Python
__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
venv/
*.whl
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
pytest-junit.xml
htmlcov/

# Env (allow *.example)
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# Frontend (future)
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/playwright-report/
frontend/test-results/

# Backend (future)
backend/.venv/

# Ops / local
ops/.env
ops/.env.*

# Makefile field-test output (M32)
out/fieldtest/

# M39 flagship proof pack (local / CI artifact)
out/flagship/

# M40 agent training program contract (local emission)
out/training_program/

# M41 replay-imitation training runs (local-only weights + JSON)
out/training_runs/

# M43 hierarchical training runs (local-only weights + JSON)
out/hierarchical_training_runs/

# M44 local live-play validation runs (local outputs + JSON)
out/live_validation_runs/
out/rl_bootstrap_runs/

# Generated SBOM (CI artifact)
sbom.json
