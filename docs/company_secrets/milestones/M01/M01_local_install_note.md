# M01 — Local SC2 install note (optional evidence)

**Date:** 2026-04-06

A **live Battle.net / StarCraft II** installation may be present on developer machines. That supports **local path detection** via `STARLAB_SC2_*` and the `starlab.sc2` probe.

**This is not governed execution proof.** Controlled, deterministic match execution remains **M02**. M01 only documents the runtime boundary, environment lock, and probe surface.

See `docs/runtime/environment_lock.md` (“Environment lock is not execution proof”).

Optional **redacted** probe output may be stored as `M01_probe_sample_redacted.json` in this folder.
