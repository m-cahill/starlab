# M04 Plan — Replay Binding to Run Identity (stub)

## Milestone identity

* **Milestone:** M04
* **Title:** **Replay Binding to Run Identity**
* **Phase:** I — Governance, Runtime Surface, and Deterministic Run Substrate
* **Suggested branch:** `m04-replay-binding` *(placeholder — set at kickoff)*
* **Suggested tag on closeout:** `v0.0.4-m04`

## Objective (draft)

Bind **replay-derived** identity (file hash, parser surface, or agreed metadata) to the **STARLAB run identity** and **lineage seed** records established in **M03**, without claiming a full **canonical run artifact** (that is **M05**) and without expanding **benchmark** semantics.

This stub exists so the next milestone has a named home. **Do not** treat this file as an approved execution plan until it is replaced or extended through milestone governance.

## Out of scope (reminders)

* **M03** already proves deterministic run identity + lineage **seed** from proof/config — **not** replay binding.
* **M05** — canonical run artifact v0 packaging.
* **Benchmark** validity — not M04.

## Hard guardrails

* **Do not** weaken CI.
* **Do not** claim canonical run artifact v0 in M04.
* **Do not** silently widen M03’s narrow proofs.

## Status

**Stub only** — seeded at **M03** closeout (**2026-04-07**). **No** M04 implementation has started.
