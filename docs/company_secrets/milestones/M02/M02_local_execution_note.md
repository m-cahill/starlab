# M02 — Local real-execution note (template)

**Status:** Fill after two local runs with `adapter: burnysc2` on the same machine.

Record:

- Date/time (UTC)
- `STARLAB_SC2_ROOT` / `STARLAB_SC2_MAPS_DIR` posture (redacted if needed)
- Map resolution mode (`explicit_path` vs `discover_under_maps_dir` vs `battle_net`)
- `pip install -e ".[sc2-harness]"` version or pin
- Command lines used for run 1 and run 2
- Whether both runs completed without error

**Do not** commit SC2 binaries, maps, or replay files.
